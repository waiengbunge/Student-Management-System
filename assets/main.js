import './styles/main.scss'
import 'bootstrap'

// Example: simple client bootstrap initialisation
document.addEventListener('DOMContentLoaded', () => {
  // future interactive widgets can be initialised here
  console.log('Acatech SMS frontend ready')
})
